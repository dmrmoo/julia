# v = v0 + a * t

# cX = (initial_velocity + acceleration)/2 * time

# cX = v0 * t + (a * t^2)/2

# v^2 = v0^2 + 2 * a * cX

println("            ----Kinematics---- 
    \nEnter initial velocity (v0), final velocity (v), 
    \nchange in x (cX), acceleration (a), and time (t):
    \n if unknown, enter ?")

